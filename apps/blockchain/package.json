{
  "name": "@land-registry/blockchain",
  "version": "1.0.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "node scripts/deploy.js",
    "consistency-check": "node scripts/consistencyCheck.js",
    "register": "node main.js",
    "start": "npm run register",
    "node": "hardhat node",
    "all": "npm run compile && npm run deploy && npm run register",
    "test": "node --check main.js && node --check scripts/deploy.js && node --check scripts/consistencyCheck.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "axios": "^1.15.1",
    "dotenv": "^17.4.2",
    "ethers": "^6.16.0",
    "form-data": "^4.0.5",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.3"
  }
}
