import StaffSearchPage from '@/app/staff/search/page';

// Reuse exactly the same component for searching and viewing records
export default function LeaderSearchPage() {
  return <StaffSearchPage />;
}
