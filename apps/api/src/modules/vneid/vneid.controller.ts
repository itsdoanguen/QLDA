import { Controller } from '@nestjs/common';

// VNeID controller placeholder — the actual VNeID sandbox runs as a
// separate service (vneid-sandbox). This controller is intentionally empty.
// Remove this file or add internal proxy endpoints here if needed in future.
@Controller()
export class VneidController {}
